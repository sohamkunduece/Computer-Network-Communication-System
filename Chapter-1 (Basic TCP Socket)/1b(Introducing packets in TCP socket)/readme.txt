This chapter introduces the basic python TCP/IP protocol via python.

we have 3 files.
1. server- A basic server which accepts TCP/IP, is waiting for communication.
2. Client- A basic client which connects to server and starts communication.
3. Packet- This  ensures all data is packed as text. Intially, this has low used as only data is transferred but packet evolves as we go throygh each chapters.

Outcome.
1. a basic boiler plate of TCP/IP communication is obtained in python.
2. Basic data transfer is obtained from it.

Requirement library:
Mostly all library are built in.

Defination Of TCP:
iT ensure scommunication...

Advantges over UDP:

Limation of the program.
Each part has to wait after sendinfg data and receive data as threading is absent

list of function use and reason:
